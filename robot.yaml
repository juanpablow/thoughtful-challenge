tasks:
  Run bot:
    robotTask: BotScraper.py
    shell: python
    workingDirectory: /tmp/robocorp-workspace
condaConfigFile: conda.yaml

artifactsDir: output

PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
